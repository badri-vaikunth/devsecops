name: terraform-workflow
on: push
jobs:
  terraform:
    runs-on: ubuntu-22.04
    steps:
      - name: version check
        run: terraform -version
      - name: init
        run: terraform init
      - name: plan 
        run: terraform plan
      - name: apply
        run: terraform apply
